config STRACE
	bool
   default y
	prompt "Compile strace utility program"
	help
	  A useful diagnostic, instructional, and debugging tool.
	  Allows you to track what system calls a program makes 
	  while it is running.

   
